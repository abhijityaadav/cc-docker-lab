// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
// next.config.mjs

// Import required modules


// Define font loading configuration
const fontConfig = {
  google: {
    families: ['Inter:wght@100..900&display=swap'], // Specify the Google Fonts family and styles
  },
};

// Export configuration with font loading settings
